# Override Rules

- Tasks with priority 10 are critical medical rescues. Always assign at least one robot to the highest-priority task immediately, regardless of distance.
- Distribute robots across zones. Always assign at least one robot to the right zone (x >= 13) and at least one robot to the left zone (x <= 6).
