"""
run.py — Simulation run entrypoint.

Usage:
    python run.py <run_folder>

    python run.py experiments/scenario_01/baseline/gpt-4o/

The run folder must contain:
    config.json   — model and scenario path
    rules.md      — override rules (empty for baseline)
    artifacts/    — written to during the run
    results.json  — written on completion

TODO: implement
"""
